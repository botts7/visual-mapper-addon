# Flows Configuration

This directory stores your custom Flow definitions.
Personal flows are ignored by git.

## Creating a new Flow
Copy `flow_example.yaml` (if available) or create a new YAML file here.
